numpy>=1.26,<3
scipy>=1.11,<2
sounddevice>=0.5,<0.6
elevenlabs>=2.65,<3
anthropic>=1.0,<2
httpx>=0.27,<1
python-dotenv>=1.0,<2
opencv-python>=4.10
pytest>=8.0,<9

# Wake word / VAD / STT (real hat.audio.listener.VoiceInput) live in
# requirements-audio.txt, not here -- that stack is still unverified on Pi
# ARM64 (ai-edge-litert aarch64 availability is unconfirmed, and
# openwakeword's speexdsp-ns dependency has no aarch64 Linux wheel at all as
# of this writing), and isn't needed for anything else in this project.
# hat.main already degrades to FakeVoiceInput if that stack can't import, so
# skip requirements-audio.txt entirely until you're specifically working on
# voice input/wake word.
